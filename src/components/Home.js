import { Component } from "react";
import { Container } from "react-bootstrap";

export class Home extends Component {
    render() {
        return (
            <Container>
                HELLO
            </Container>
        )
    }
}